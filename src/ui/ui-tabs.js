/*global al */

//tabs
(function () {

    al.ui.register('tabs', function (properties, element, scope, env) {

        $(element).tab();

        return {
            
        };

    }, false);

}());
